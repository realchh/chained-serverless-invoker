# Serverless Tuner Middleware

This subpackage contains the offline middleware utilities:

- Parsing invoker logs
- Aggregating edge/node latency statistics
- Critical-path computation and config rewriting
- Benchmark helpers and baseline data

For full repository documentation and research context, see the root-level `README.md`.
